# Store real credentials in your secrets manager (Vault, AWS Secrets Manager, etc.)
# and inject them as k8s Secrets via External Secrets Operator or similar.
# The values below are base64-encoded placeholders — replace before deploying.
#
# To encode: echo -n "yourvalue" | base64
apiVersion: v1
kind: Secret
metadata:
  name: recsys-secrets
  namespace: recsys
type: Opaque
data:
  POSTGRES_PASSWORD: cmVjc3lz        # recsys
  MINIO_ACCESS_KEY: bWluaW8=         # minio
  MINIO_SECRET_KEY: bWluaW8xMjM=    # minio123
  API_KEY: ""                        # base64 of your key: echo -n "your-key" | base64
